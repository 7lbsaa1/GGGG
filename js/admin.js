import {requireUser,nav,logout,toast} from "./navigation.js";import {db} from "./firebase.js";import {ref,get} from "https://www.gstatic.com/firebasejs/11.0.2/firebase-database.js";
(async()=>{const me=await requireUser();const [us,ps,rs]=await Promise.all([get(ref(db,"users")),get(ref(db,"posts")),get(ref(db,"reports"))]);const users=us.val()||{},posts=ps.val()||{},reports=rs.val()||{};document.getElementById("app").innerHTML=nav(me.data)+`<main class="admin-shell"><h1>لوحة الإدارة</h1><nav class="admin-nav"><a href="admin.html">الرئيسية</a><a href="users.html">المستخدمون</a><a href="reports.html">البلاغات</a><a href="usercredentials.html">التوثيقات</a><a href="home.html">الموقع</a></nav><div class="admin-grid"><div class="stat-card"><span>المستخدمون</span><strong>${Object.keys(users).length}</strong></div><div class="stat-card"><span>المنشورات</span><strong>${Object.keys(posts).length}</strong></div><div class="stat-card"><span>البلاغات</span><strong>${Object.keys(reports).length}</strong></div><div class="stat-card"><span>موثقون</span><strong>${Object.values(users).filter(x=>x.verified).length}</strong></div><div class="stat-card"><span>محظورون</span><strong>${Object.values(users).filter(x=>x.blocked).length}</strong></div></div></main>`})()