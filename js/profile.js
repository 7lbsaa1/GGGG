import {requireUser,nav,toast} from "./navigation.js";import {db} from "./firebase.js";import {ref,get,update} from "https://www.gstatic.com/firebasejs/11.0.2/firebase-database.js";
const esc=s=>(s||"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]));
const verified=u=>u?.verified?'<img class="verified" src="https://raw.githubusercontent.com/7lbsaa1/assets/main/facebook-verified.png" alt="موثق">':"";
(async()=>{const me=await requireUser(),u=me.data;const snap=await get(ref(db,"posts"));const all=snap.val()||{};const own=Object.values(all).filter(p=>p.userId===me.auth.uid).sort((a,b)=>b.createdAt-a.createdAt);document.getElementById("app").innerHTML=nav(u)+`<main class="profile-wrap"><section class="cover glass"><img src="${u.coverImage||'https://images.unsplash.com/photo-1534088568595-a066f410bcda?auto=format&fit=crop&w=1400&q=80'}" alt="الغلاف"></section><section class="profile-head"><img class="profile-avatar" src="${u.profileImage||'https://via.placeholder.com/200'}"><div class="profile-info"><h1>${esc(u.name)} ${verified(u)}</h1><div class="muted">@${esc(u.username)}</div><p class="bio">${esc(u.bio)}</p><div class="profile-stats"><span>${own.length} منشور</span><span>${u.friendsCount||0} صديق</span></div></div><div class="profile-actions"><button class="primary-btn" id="edit">تعديل الملف</button></div></section><section class="panel glass"><h2>الصور والمنشورات</h2><div class="profile-grid">${own.map(p=>`<img loading="lazy" src="${p.imageBase64}" alt="${esc(p.caption)}">`).join("")||'<p class="muted">لم تنشر صورًا بعد.</p>'}</div></section></main>`;document.getElementById("edit").onclick=()=>{const n=prompt("الاسم",u.name);if(n===null)return;const bio=prompt("النبذة",u.bio||"");update(ref(db,"users/"+me.auth.uid),{name:n,bio:bio??u.bio}).then(()=>location.reload())}})();